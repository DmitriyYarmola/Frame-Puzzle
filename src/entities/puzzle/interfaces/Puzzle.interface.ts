export type PuzzleEdge = "top" | "right" | "bottom" | "left" | "center";

export type PuzzleEdges = PuzzleEdge[];


export type PuzzleSocket = "inside" | "outside";

export type PuzzleSockets = (PuzzleSocket | null)[]
